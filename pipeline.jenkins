#!groovy

pipeline {
    agent any

    triggers {
        pollSCM('* * * * *')
    }

    options {
        timestamps()
    }

    stages {
        stage('Build') {
            steps {
                sh "mvn clean package"
                sh "docker build . -t show-class-info"
            }
        }
    } 
}